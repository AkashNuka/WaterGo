
services:
  web:
    build: .
    container_name: watergo_web
    command: gunicorn watergo.wsgi:application --bind 0.0.0.0:8000
    ports:
      - "8000:8000"
    env_file:
      - .env
    volumes:
      - .:/app
